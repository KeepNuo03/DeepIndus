Put local YOLO model artifacts in this directory:

1) yolo11n_induscore.tflite
2) labels.txt (one class name per line, same order as training class ids)

This folder is used by the Android local inference engine scaffold.
