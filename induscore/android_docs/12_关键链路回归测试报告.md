# 关键链路回归测试报告

## 目标
完成移动端关键链路回归验证，覆盖：
- 登录与鉴权
- 任务列表/详情/复核提交
- 上传入队/状态查询/重试恢复
- 构建与单元测试稳定性

## 测试范围
- 代码构建回归：`assembleDebug`
- 单元测试回归：`testDebugUnitTest`
- 链路脚本回归（手工执行建议）：
  - 登录 -> 系统主页 -> 复核任务 -> 复核详情 -> 提交
  - 抽检上传 -> 上传状态查询 -> 失败任务手动重试

## 自动化测试用例（本次新增）
1. `android-app/app/src/test/java/com/induscore/mobile/ui/state/ErrorMessageMapperTest.kt`
   - 覆盖 401/403/404/500 + 网络异常用户提示映射。
2. `android-app/app/src/test/java/com/induscore/mobile/domain/MobileRepositoryReviewMappingTest.kt`
   - 覆盖复核提交字段映射（`approve -> confirm`）。

## 执行命令
在 `android-app/` 目录执行：
- `.\gradlew.bat -g "C:\gradle-home" :app:testDebugUnitTest --no-daemon`
- `.\gradlew.bat -g "C:\gradle-home" :app:assembleDebug --no-daemon`

如遇 `Unable to delete ... test-results/testDebugUnitTest/binary/output.bin`，先清理后重跑：
- `Remove-Item "app\build\test-results\testDebugUnitTest\binary" -Recurse -Force`

## 结果
- 单元测试代码：已新增并通过编译（`compileDebugUnitTestKotlin`）
- 单元测试执行：已通过（`testDebugUnitTest`）
- Debug 构建：通过（`assembleDebug`）
- 关键链路状态：可回归（含弱网失败恢复入口）

## 结论
关键链路回归测试能力已落地（自动化用例 + 手工回归路径），且已完成 CLI 自动化测试全绿验证。
当前版本满足继续推进下一阶段（网络类型差异化策略与试点压测）。

## 后续补充建议
- 增加仪表测试（androidTest）覆盖真实页面交互。
- 增加上传队列操作日志断言（覆盖重试/清理动作与状态统计一致性）。
- 已新增真实产线试点压测执行包：
  - `14_真实产线试点压测方案.md`
  - `15_真实产线试点压测记录模板.md`
  - 下一步在现场环境执行并回填指标结果（闭环率/恢复率/P95）。
